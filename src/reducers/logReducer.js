const initialState = {
	logs: null,
	current: null,
	loading: false,
	error: null,
}

const logReducer = (state = initialState, action) => {
	switch (action.type) {
		default:
			return state
	}
}

export default logReducer
